# Consolidated research-run imports

Curated content from the autonomous research branches, imported verbatim
(paths preserved) from their source refs. Only material the source branch
itself treats as proved, plus clearly stated hypotheses and the run ledgers,
is included; exploration scripts and files that served only unproved
attempts were left on the source branches.

| Folder | Source ref | Tip | What it is |
|---|---|---|---|
| `autoresearch/` | `autoresearch` | 82fee4e (2026-06-25) | Local autonomous run: lemmas L13-L50 extending the Lean-verified L1-L12 base, its own folder taxonomy, BLUEPRINT and research log. |
| `autoresearch-snellius/` | `snellius/autoresearch` | 8323482 (2026-06-26) | Cluster autonomous run from the same L1-L12 base: L13-L43 range with tChow, flattening sign-rank, shatter-rectangle, Warren counting, INT and IDX families. |

Numbering caveat: both runs (and the main `lemmas/` stack) claim overlapping
numeric lemma IDs with different content. IDs are only meaningful inside one
folder namespace; the top-level `CONSOLIDATION.md` index disambiguates.
